{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Debug",
            "command": "make",
            "args": [
                "debug"
            ],
            "options": {
                "cwd" : "${workspaceFolder}"
            }
        },

        {
            "type": "shell",
            "label": "Build Test",
            "command": "make",
            "args": [
                "test"
            ],
            "options": {
                "cwd" : "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}